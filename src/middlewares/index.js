const fieldsValidation = require('./createAccount/fieldsValidation');
const emailValidation = require('./createAccount/emailValidation');
const passwordValidation = require('./createAccount/passwordValidation');

module.exports = {
  fieldsValidation,
  emailValidation,
  passwordValidation,
};